---
tags:
  - технический-долг
  - roadmap
date: 2026-07-02
---

# Технический долг

Реестр технического долга. Формат: описание → риск → действие → приоритет.

Баги → [[известные баги]]
Валидационные кейсы → [[валидационные кейсы]]

---

### DEBT-002: wstring в ядре
- **Описание:** часть ядра использует `std::wstring` (наследие Windows GUI)
- **Риск:** невозможность сборки под Linux, усложнение интерфейсов
- **Действие:** заменить на `std::string` (UTF-8)
- **Приоритет:** средний (блокирует кроссплатформенность)

### DEBT-003: ReservoirSimulator — монолит ~1361 строк (РАЗБИТ)
- **Описание:** один класс содержит Newton loop, time stepping, assembly, solve, boundary conditions
- **Риск:** любое изменение в одном аспекте затрагивает весь файл, сложно тестировать изолированно
- **Действие:** разбит на DEBT-054..057 (послойно, от внутреннего к внешнему)
- **Приоритет:** высокий (блокирует unit-тестирование ядра)
- **План:** [[debt-003 refactoring-reservoir-simulator]]
- **Статус:** ✅ завершено 2026-07-14 (все 4 этапа: DEBT-054, DEBT-055, DEBT-056, DEBT-057)

### DEBT-004: DB-фабрики заглушены
- **Описание:** `ConnectionFactory`, `ReservoirFactory`, `HorizonFactory`, `WellFactory`, `RawWellFactory` — пустые заглушки после отключения PostgreSQL
- **Риск:** мёртвый код, путаница
- **Действие:** удалить при переходе на файловый ввод (фаза 7)
- **Приоритет:** низкий (не мешает)

### DEBT-005: UniversalSVParser/Writer исключены из сборки
- **Описание:** cp1251 кодировка несовместима с `/utf-8`. Файлы есть, но не компилируются
- **Действие:** переписать на UTF-8 или удалить, если файловый ввод пойдёт через JSON
- **Приоритет:** низкий

### DEBT-006: PI-контроллер выключен по умолчанию
- **Описание:** реализован и имеет 10 юнит-тестов, но `use_pi_controller_ = false`
- **Причина:** не верифицирован на нестационарных задачах (breakthrough, well switching)
- **Действие:** верифицировать → включить
- **Приоритет:** средний
- **Подробности:** [[PI-контроллер safety=1 и target=12 для Newton-based timestep control]]

### DEBT-007: AMGCL — локальная копия вместо FetchContent
- **Описание:** библиотека AMGCL (header-only, MIT) хранится как локальная копия в `HydroSolver/AMGSolver/amgcl/`. Репозиторий upstream: `https://github.com/ddemidov/amgcl`
- **Локальные изменения:** два файла: (1) `amgcl/relaxation/ilu0.hpp:156-157` — `precondition(!math::is_zero((*D)[i]), "Zero pivot in ILU")` заменён на fallback `(*D)[i] = 1` (BUG-002 проявление 1); (2) `amgcl/preconditioner/cpr.hpp:522-523` — `assert(!math::is_zero(d))` заменён на fallback `d = 1` + запись в массив (BUG-002 проявление 3)
- **Также:** версия 2022 vs legacy 2020 — добавлены `col_type`/`ptr_type` typedefs, zero-stripping в L/U, `static_cast<ptrdiff_t>` в циклах, допуск параметра `k` в params
- **Риск:** устаревшая версия, невозможность обновления без ручного merge, патч не документирован
- **Действие:** ~~перейти на `FetchContent`~~ → заменено на DEBT-051 (git submodule через форк)
- **Приоритет:** средний (блокирует обновление amgcl)
- **Подробности:** [[локальные патчи AMGCL для GDM]]
- **Статус:** 🔄 ЗАМЕНЕНО → DEBT-051

### DEBT-008: Eigen — мёртвый include без использования
- **Описание:** `Maps.h` включает `<Eigen/Eigen>`, но ни один тип или функция Eigen не используется нигде в проекте. Workaround `_HAS_DEPRECATED_RESULT_OF=1` в CMake — только ради этого include
- **Репозиторий upstream:** `https://gitlab.com/libeigen/eigen` (GitLab, MPL2)
- **Риск:** сборка `gdm_data` зависит от наличия Eigen на машине; workaround загрязняет CMake
- **Действие:** удалить `#include <Eigen/Eigen>` из Maps.h и `_HAS_DEPRECATED_RESULT_OF=1` из CMakeLists.txt
- **Приоритет:** низкий (Eigen не используется, gdm_data не собирается)
- **План:** [[debt-008 eigen-remove-dead-include]]
- **Статус:** ✅ удалено 2026-07-08

### DEBT-009: тестовые и отладочные артефакты в cwd
- **Описание:** код в `MER_Descriptor.cpp`, `MatrixCSR.cpp`, `LinearProblem.cpp`, `SparsityPattern.cpp`, `AbstractGrid.h` пишет отладочные файлы `test_*.txt` в текущую рабочую директорию. Тест `test_buckley_leverett.cpp:507` пишет `bl_validation_profile.csv` туда же
- **Файлы:** `test_MER_*.txt`, `test_connections.txt`, `test_Matrix.txt`, `test_diagValues.txt`, `test_SparsityPattern*.txt`, `test_RHS.txt`, `test_Corrections.txt`, `bl_validation_profile.csv`, `WellTestData/`
- **Контроль версий:** файлы в `.gitignore` — не отслеживаются ✅
- **Проблема:** засоряют корень проекта; пути не настраиваемые; отладочный дамп включён в production-коде (нет `#ifdef DEBUG`)
- **Действие:** обернуть в `#ifdef GDM_DUMP_DEBUG` или вынести за флаг; если оставлять — писать в `build/debug_dump/`; CSV валидации — в `build/validation/`
- **Приоритет:** низкий

### DEBT-010: `new`/`delete` для скважин вместо `unique_ptr`
- **Описание:** `std::map<WellName, wells::SomeWell*>` с ручным `new` при вставке и `delete` в деструкторе
- **Риск:** утечка памяти при исключении между `new` и `insert`; отсутствие move-семантики
- **Действие:** заменить на `std::map<WellName, std::unique_ptr<wells::SomeWell>>`
- **Файл:** `ReservoirSimulator.cpp:145-156`
- **Приоритет:** средний
- **Подробности:** [[code-review-2026-06-28-утечки-ресурсов-и-память]]

### DEBT-011: `static` PVT-свойства в `PhysPropCell`
- **Описание:** `static std::array<double, 8> ConstantPointProperties` — все ячейки разделяют один набор PVT-констант
- **Риск:** невозможность запуска параллельных симуляций; race condition при OpenMP; неявная глобальная зависимость
- **Действие:** передавать PVT через конструктор или shared-контекст (не static)
- **Файл:** `AbstractCells.h:148`, `AbstractCells.cpp:7`
- **Приоритет:** средний (блокирует OpenMP и мульти-модельные расчёты)
- **Подробности:** [[code-review-2026-06-28-утечки-ресурсов-и-память]]

### DEBT-012: `vector` аллокация внутри тройного цикла в `AccountForBoundaryConditions`
- **Описание:** на каждой итерации по `(i,j,k)` создаётся `std::vector<double>(blockSize)` и `std::vector<double>(B)` через heap-аллокацию
- **Риск:** тысячи heap-аллокаций на Newton step (51×51×4 = ~400 граничных ячеек × 2 направления)
- **Действие:** заменить на `std::array<double, 4>` / `double[4]` (как в `fillMatrixBlockRow`)
- **Файл:** `ReservoirSImulator.h:145-146`
- **Приоритет:** средний (производительность)
- **Подробности:** [[code-review-2026-06-28-утечки-ресурсов-и-память]]

### DEBT-013: `OverallFluxes()` — O(N) heap-аллокаций через вложенные `push_back`
- **Описание:** строит 4 трёхмерных `vector<vector<vector<double>>>` по-элементно, ~40K аллокаций для 51×51×4
- **Риск:** производительность; метод вызывается каждый шаг по времени
- **Действие:** предвыделять размеры; или хранить flux-поле как flat-array
- **Файл:** `ReservoirSimulator.cpp:794-1050`
- **Приоритет:** средний
- **Подробности:** [[code-review-2026-06-28-утечки-ресурсов-и-память]]

### DEBT-014: Дублирование ~450 строк логики потоков
- **Описание:** `OverallFluxes()`, `OilContourFlux()`, `WaterContourFlux()` содержат одинаковую структуру: тройной цикл, upstream-выбор, умножение на подвижность
- **Действие:** параметризовать обход граней, объединить в один шаблонный метод
- **Файл:** `ReservoirSimulator.cpp:794-1247`
- **Приоритет:** средний (связан с DEBT-003 рефакторингом ReservoirSimulator)
- **Подробности:** [[code-review-2026-06-28-архитектурные-ограничения]]

### DEBT-015: Все поля `ReservoirSimulator` — public
- **Описание:** `Grid`, `MyProblem`, `Wells`, accumulators, `RefPressure` — всё public
- **Риск:** любой клиент может сломать инвариант; API хрупкий
- **Действие:** выделить private members + публичные методы (часть DEBT-003)
- **Файл:** `ReservoirSImulator.h:39-72`
- **Приоритет:** средний
- **Подробности:** [[code-review-2026-06-28-архитектурные-ограничения]]

### DEBT-016: `B = 2` захардкожен в нескольких классах
- **Описание:** число уравнений на ячейку (`B = 2`) дублировано в `LinearProblem`, `ReservoirSimulator`, `SomeWell` как `static constexpr`/`const int`
- **Риск:** расширение до трёхфазной модели или добавление уравнения энергии потребует grep + замену
- **Действие:** единая constexpr-константа в общем заголовке или template-параметр
- **Приоритет:** низкий (до расширения модели)
- **Подробности:** [[code-review-2026-06-28-архитектурные-ограничения]]

### DEBT-017: `mutable` / `const_cast` вместо честной const-корректности
- **Описание:** `MER_Data` — 7+ `mutable` полей, `const` методы мутируют через `mutable`. `WellJobs::RawWellPerforationData` — `mutable`. `SomeWell` — `const_cast`
- **Риск:** thread-unsafe; `const` — фасад, не контракт; мешает параллелизации
- **Действие:** разделить на const-query и non-const-mutation; кэшированное состояние MER вынести в отдельный mutable-объект
- **Приоритет:** средний
- **Подробности:** [[code-review-2026-06-28-архитектурные-ограничения]]

### DEBT-018: Мёртвый код — ~200 строк закомментированного кода
- **Описание:** NeuralNetwork SaveWeights/LoadWeights (110 строк), IRAP/JSON/LogFile (70 строк), `if (false && ...)`, пустой `update_currentIntegrationTau`, некорректные закомментированные assert-ы
- **Действие:** удалить
- **Файлы:** `ReservoirSimulator.cpp:282-350, 1141, 1253-1361`, `NumericalParameters.h:105`, `AbstractCells.h:60-61, 82-83, 110-111`
- **Приоритет:** низкий
- **Подробности:** [[code-review-2026-06-28-мёртвый-код-и-гигиена]]

### DEBT-019: Magic numbers для индексации свойств ячейки
- **Описание:** свойства хранятся в `vector<double>` / `array<double>` с доступом по числовым индексам (`ConstantFieldProperties[7]` = PoreVolume, `DependentFieldProperties[3]` = F_Oil)
- **Риск:** при изменении порядка — молчаливая ошибка; accessor-методы скрывают проблему
- **Действие:** enum или именованная структура вместо индексов
- **Файл:** `TwoPhaseFlowCell.cpp`, `AbstractCells.h`
- **Приоритет:** низкий
- **Подробности:** [[code-review-2026-06-28-архитектурные-ограничения]]

### DEBT-020: Глубокая иерархия наследования ячеек (6 уровней)
- **Описание:** `SomeDimCell → Dim1Cell → Dim2Cell → Dim3Cell`, плюс множественное наследование `SomeProcessCell_TimeDependent<Dim3Cell>` от `Dim3Cell`, `TimeDependentCell`, `PhysPropCell`
- **Риск:** сложность, Dim1/Dim2 никогда не инстанцируются; можно заменить одной структурой
- **Действие:** collapsed cell hierarchy
- **Приоритет:** низкий
- **Подробности:** [[code-review-2026-06-28-архитектурные-ограничения]]

### DEBT-021: `int` переменные цикла итерируют по `size_t` границам (~30 мест)
- **Описание:** по всей кодовой базе циклы `for (int l = 0; l < ActiveCellsNmbr(); l++)`, `for (int i = 0; i < nx; ...)` и т.п. — `int` сравнивается с `size_t`. При `> 2³¹` ячейках — UB, но даже сейчас — предупреждения компилятора C4018/C4267
- **Файлы:** `AbstractGrid.h:39,47,55,62,138`, `ReservoirSimulator.cpp:170,195,204,464,683,717`, `FluxReader.h:71-77`, `SomeWell.cpp:385`, `ReservoirSImulator.h:204-208`
- **Действие:** заменить `int` на `size_t` в циклах, где граница — `size_t`
- **Приоритет:** средний (шум warnings + потенциальный UB)

### DEBT-022: `activeCellsNmbr` — `int`, а `ActiveCellsNmbr()` возвращает `size_t`
- **Описание:** поле `int activeCellsNmbr = 0` в `AbstractGrid.h:29`, геттер `size_t ActiveCellsNmbr()` на строке 122 — неявное расширение `int → size_t`. При отрицательном значении (баг) — огромный `size_t`
- **Файл:** `HydroSolver/Solver/Grids/AbstractGrid.h:29,122`
- **Действие:** тип поля → `size_t`
- **Приоритет:** средний

### DEBT-023: 5+ методов `AbstractGrid` без `const` (только чтение)
- **Описание:** `GetNeighboursPointer(int l)`, `CommonEdgeArea(int l)`, `GetNeighboursIdx(int l)`, `GetAreas(int l)`, `GetConnectivityGraph()` — все возвращают данные без модификации, но не объявлены `const`. Это мешает вызывать их через `const&` на сетку
- **Файл:** `HydroSolver/Solver/Grids/AbstractGrid.h:59-73`
- **Действие:** добавить `const`
- **Приоритет:** средний

### DEBT-024: `SomeWell::NmbrOfOpenedCells()` без `const`
- **Описание:** метод подсчитывает открытые перфорации, не мутирует состояние, но не помечен `const`
- **Файл:** `HydroSolver/Reservoir/Well/SomeWell.h:156`
- **Действие:** добавить `const`
- **Приоритет:** низкий

### DEBT-025: `SomeFlowField` — `operator()`, `t()`, `V()` без `const`
- **Описание:** `FlowFieldSnapshot::operator()(const Point&)` — интерполяция, `t()` — геттер времени, `SomeField2D::V(x,y)` — вычисление скорости. Все read-only, но не `const`
- **Файл:** `HydroSolver/Anomaly/FlowField/SomeFlowField.h:48,102,103`
- **Действие:** добавить `const`
- **Приоритет:** низкий

### DEBT-026: `AddOffDiagBlock` — `vector<double>&` без `const` (в отличие от `AddDiagBlock`)
- **Описание:** `LinearProblem::AddOffDiagBlock(size_t, int, vector<double>&)` принимает данные по мутабельной ссылке, хотя `AddDiagBlock` принимает `const vector<double>&`. `MatrixCSR::AddOffDiagBlock` тоже без `const`. Данные не модифицируются
- **Файл:** `LinearProblem.h:94`, `MatrixCSR.h:52`
- **Действие:** `std::vector<double>&` → `const std::vector<double>&`
- **Приоритет:** средний

### DEBT-027: Передача по значению вместо `const&` — строки и вектора
- **Описание:** `CheckForNAN(std::vector<double> someArr)` копирует весь вектор. `printPointVariable(const wstring fName, ...)` и `printFieldVariable(const wstring fName, ...)` копируют wstring. Для больших сеток — O(N) лишних аллокаций
- **Файл:** `ReservoirSImulator.h:111,374,378`
- **Действие:** параметры → `const&`
- **Приоритет:** средний

### DEBT-028: `AddOffDiagBlock` — `int neibIdx` vs `size_t` в `MatrixCSR`
- **Описание:** `LinearProblem::AddOffDiagBlock(size_t l, int neibIdx, ...)` передаёт `int neibIdx` в `MatrixCSR::AddOffDiagBlock(size_t, size_t, ...)` — неявное приведение `int → size_t` при каждом вызове
- **Файл:** `LinearProblem.h:94-95`, `MatrixCSR.h:52`
- **Действие:** `int neibIdx` → `size_t neibIdx` в `LinearProblem`
- **Приоритет:** средний

### DEBT-029: `catch (std::exception err)` — ловля по значению, slice
- **Описание:** 6 мест ловят `std::exception` по значению: `DBUtils.cpp:30`, `WellDataHandler.cpp:16,267,294`, `WellsBase.cpp:123`, `UniversalSVParser.cpp:170`. Производные типы (с дополнительными полями) теряют информацию при копировании
- **Действие:** `std::exception err` → `const std::exception& err`
- **Приоритет:** низкий

### DEBT-030: `using namespace` в заголовочных файлах
- **Описание:** `ReservoirSImulator.h:27-29`, `AbstractGrid.h:8`, `OilField.h:9`, `MathRoutines.h:9` — `using namespace` загрязняет пространство имён каждого включающего файла
- **Действие:** убрать `using namespace` из `.h`, использовать квалифицированные имена или `using`-объявления в `.cpp`
- **Приоритет:** средний

### DEBT-031: 10 `const_cast` в production-коде — обход const-контракта
- **Описание:** `SomeWell.cpp:143,315,317,337,339`, `RawWellFactory.cpp:56,83,112,113`, `MER_Descriptor.cpp:99`. BUG-010 покрывает только строку 143. Остальные 9 — те же паттерны: метод объявлен `const`, но мутирует через `const_cast`. Часть из них — перфорации (MoveDate, AssembleJobDates), часть — фабрики (mer, gisd, pfd)
- **Действие:** пересмотреть const-контракты: либо убрать `const` с методов, либо сделать API неконстантным
- **Приоритет:** средний (thread-safety, UB если объект был создан как `const`)

### DEBT-032: Утечка `_locale_t` в `DBUtils.h`
- **Описание:** `_create_locale(LC_NUMERIC, "en-US")` на строке 269 создаёт locale как поле класса, но нигде не вызывается `_free_locale`. При каждом создании экземпляра — утечка
- **Файл:** `HydroSolver/Data/DBUtils.h:269`
- **Действие:** вызывать `_free_locale` в деструкторе или использовать RAII-обёртку
- **Приоритет:** низкий

### DEBT-033: `reinterpret_cast<int*>(var)` — strict aliasing violation в WellDataHandler
- **Описание:** `char* var = new char[N]`; затем `*reinterpret_cast<int*>(var)`, `*reinterpret_cast<float*>(var)`, `*reinterpret_cast<double*>(var)`. Нарушает strict aliasing (UB по стандарту). Корректная альтернатива — `std::memcpy`
- **Файл:** `HydroSolver/Utils/WellDataHandler.cpp:49,53,57,61`
- **Действие:** заменить `reinterpret_cast` на `std::memcpy`
- **Приоритет:** средний (MSVC допускает, но UB по стандарту)

### DEBT-034: Двойные точки с запятой `;;` в SomeWell.cpp
- **Описание:** `return overallDebit * f_oil_in;;` (строка 171), `return overallDebit * (1 - f_oil_in);;` (строка 179), `return str;; }` (строка 67). Безвредно, но шум
- **Файл:** `HydroSolver/Reservoir/Well/SomeWell.cpp:67,171,179`
- **Действие:** убрать лишние `;`
- **Приоритет:** низкий

### DEBT-035: `f_oil = f_oil = expr` — двойное присваивание (опечатка)
- **Описание:** `f_oil = f_oil = cell.F_Oil()` на строках 908 и 1029. Работает корректно (присваивает одно и то же значение дважды), но выглядит как ошибка копипаста
- **Файл:** `ReservoirSimulator.cpp:908,1029`
- **Действие:** убрать дублирование
- **Приоритет:** низкий

### DEBT-036: `std::exception("string")` — MSVC-расширение, непортируемо (29 мест)
- **Описание:** `throw std::exception("message")` — стандарт C++ не определяет конструктор `std::exception(const char*)`. Работает только на MSVC. При портировании на GCC/Clang — ошибка компиляции
- **Файлы:** 12 файлов, 29 вхождений (MathRoutines.h, JSONCreate.cpp, ModelHandler.cpp, BinaryFileHandler.cpp и др.)
- **Действие:** заменить на `throw std::runtime_error("message")` или `std::invalid_argument`
- **Приоритет:** средний (портируемость)

### DEBT-037: 168 `virtual` методов, только 2 `override`
- **Описание:** наследники не используют спецификатор `override`. Если сигнатура виртуального метода в базе изменится, наследник создаст новый метод вместо переопределения — молчаливая ошибка, не ловится компилятором
- **Действие:** добавить `override` ко всем переопределённым виртуальным методам
- **Приоритет:** средний

### DEBT-038: `SomeGrid::~SomeGrid()` не виртуальный, но класс наследуется
- **Описание:** `SomeGrid` деструктор (строка 155) не `virtual`, но `SomeStructuredGrid3Dim` наследует от `SomeGrid`. Удаление через указатель на базу — UB. Шаблонный класс, поэтому delete через базу маловероятен, но опасен
- **Файл:** `AbstractGrid.h:155,161`
- **Действие:** `virtual ~SomeGrid() = default;` или `protected` деструктор
- **Приоритет:** низкий

### DEBT-039: `AMG_maxSolverIterCount` — `double` используется как целочисленный лимит итераций
- **Описание:** `AMG_maxSolverIterCount` — `double`, модифицируется как `+= 0.4`. Геттер `CurrentAMG_maxSolverIterationCount()` возвращает `size_t` через `(int)round(...)`. Если значение станет отрицательным → `(int)round()` отрицателен → implicit cast в `size_t` → огромный лимит
- **Файл:** `NumericalParameters.h:29,66`, `NumericalParameters.cpp:21`
- **Действие:** заменить `double` на `int` или `size_t`, арифметику адаптации — целочисленной
- **Приоритет:** средний

### DEBT-040: Мёртвые методы и пустые тела
- **Описание:** `update_currentIntegrationTau(double tau) {}` — метод с пустым телом, нигде не вызывается (`NumericalParameters.h:105`). `FluxReader` — целый `static class` (MSVC-расширение), не включён в CMake, не используется. `CacheFile.h` — не в сборке
- **Действие:** удалить неиспользуемый код
- **Приоритет:** низкий

### DEBT-041: Дублирование логики записи бинарного заголовка в Save-функциях
- **Описание:** `SaveFlowField2File_bin` (строки 672-685) и `SaveSaturationPressure_bin` (строки 706-719) содержат идентичный код записи заголовка (time frame + grid dimensions). Copy-paste — источник расхождений при изменении формата
- **Файл:** `HydroSolver/Reservoir/ReservoirSimulator.cpp:672-685, 706-719`
- **Действие:** выделить `WriteBinaryHeader()` и `ReadBinaryHeader()`
- **Приоритет:** средний
- **Обнаружено при анализе:** BUG-012

### DEBT-042: Отсутствие `SomeFlowField::read` — асимметрия write/read
- **Описание:** `SomeFlowField::write(ofstream&)` существует, но парного `read(ifstream&)` нет. `LoadFlowFieldFromFile_bin` дублирует логику чтения inline (~30 строк), что привело к BUG-012 (расхождение типов)
- **Файл:** `HydroSolver/Anomaly/FlowField/SomeFlowField.cpp`, `HydroSolver/Reservoir/ReservoirSimulator.cpp:749-780`
- **Действие:** добавить `SomeFlowField::read(ifstream&)` и `FlowFieldSnapshot::read(ifstream&)`, заменить inline-код в Load
- **Приоритет:** средний
- **Обнаружено при анализе:** BUG-012

### DEBT-043: Мёртвый код `LoadFlowFieldFromFile` (без `_bin`)
- **Описание:** `ReservoirSimulator::LoadFlowFieldFromFile(const wstring&)` (строка 723) — пустое тело с TODO-комментарием. Объявлен в заголовке, но нигде не вызывается
- **Файл:** `HydroSolver/Reservoir/ReservoirSimulator.cpp:723-726`, `HydroSolver/Reservoir/ReservoirSImulator.h`
- **Действие:** удалить метод из .cpp и .h
- **Приоритет:** низкий
- **Обнаружено при анализе:** BUG-012

### DEBT-044: `SparsityPattern` конструктор пишет файлы в cwd
- **Описание:** конструктор `SparsityPattern` безусловно вызывает `printPattern()`, `printDiagonalBlocks()`, `printOffDiagBlocks()`, которые создают файлы `test_SparsityPattern.txt`, `test_SparsityPattern_DiagBlocks.txt`, `test_SparsityPattern_OffDiagBlocks.txt` в текущей рабочей директории. При запуске тестов файлы засоряют cwd; при нескольких экземплярах — гонка за файл
- **Файл:** `HydroSolver/Solver/Math/SparsityPattern.cpp:14-35,159-161`
- **Действие:** (1) убрать print-вызовы из конструктора, (2) если дамп нужен для отладки — обернуть в `#ifdef GDM_DEBUG_SPARSITY` или вынести в отдельный метод, (3) каждый исполняемый файл должен писать свои результаты в уникальную директорию (`build/<target>/output/` или аналог), а не в cwd
- **Приоритет:** средний
- **Статус:** ✅ исправлено 2026-06-29 (print-вызовы убраны из конструктора в рамках BUG-019)
- **Связано с:** [[BUG-019]], [[DEBT-009]]

### DEBT-045: Рефакторинг интерфейса `SparsityPattern` — const/noexcept, инициализация, контейнеры
- **Описание:** класс `SparsityPattern` не соответствует best practices C++: (1) `const`/`noexcept` — геттеры (`Row()`, `Col()`, `DiagBlocks()`, `OffDiagBlocks()`, `NmbrOfElementsAboveBlockRow()`, `TotalNmbrOfBlocks()`, `NmbrOfNonzerosPerUnitBlock()`) не помечены `noexcept`; print-методы принимают мутабельные ссылки где можно `const`; (2) инициализация полей — часть полей (`row_raw`, `col_raw`, `diagBlocks_raw`, `offDiagBlocks_raw`, `elementsAboveBlockRow`) заполняется в теле конструктора через `push_back`/`resize`, хотя `blockSize`, `totalNmbrOfBlocks` можно вычислить в member-initializer list; `elementsAboveBlockRow` пересоздаётся через `= vector(...)` вместо инициализации; (3) контейнеры — `blockPattern` хранится как `vector<bool>` (proxy-тип с проблемами), `blocksPerRow` можно заменить на `vector<uint16_t>` или аналог; `connectivityGraph` принимается как `vector<vector<int>>` — рассмотреть CSR-представление или `span`; для фиксированных размеров (blockPattern при известном eqNmbr) рассмотреть `std::array`
- **Файлы:** `HydroSolver/Solver/Math/SparsityPattern.h`, `HydroSolver/Solver/Math/SparsityPattern.cpp`
- **Действие:** (1) добавить `noexcept` на все геттеры и `const` где применимо; (2) пересмотреть инициализацию полей — максимум в member-initializer list; (3) оценить замену `vector<bool>` на `vector<char>` или `std::bitset`; (4) оценить замену `vector<vector<int>>` для графа на CSR или flat-представление; (5) для blockPattern при фиксированном eqNmbr ≤ 4 рассмотреть `std::array<bool, N*N>`
- **Приоритет:** средний
- **Связано с:** [[BUG-013]], [[DEBT-044]]
- **Статус:** 🔴 ОТКРЫТ

### DEBT-046: Тесты `SparsityPattern` — недостаточное покрытие конфигураций и полей
- **Описание:** все 6 тестов используют только 1D-конфигурации (цепочки ячеек). Нет 2D (сетка NxM) и 3D (NxMxK) графов связности. Проверки в каждом тесте охватывают лишь единичные поля класса (`TotalNmbrOfBlocks`, `Row().size()`, `DiagBlocks().size()`), не проверяя содержимое `col_raw`, `diagBlocks_raw`, `offDiagBlocks_raw`, `elementsAboveBlockRow` поэлементно. Для каждой конфигурации (граф + blockPattern) спарсити-паттерн детерминирован — нужно восстановить ожидаемую CRS-структуру и сопоставить с каждым полем объекта
- **Файл:** `tests/unit/math/test_SparsityPattern.cpp`
- **Действие:** (1) добавить тесты с 2D-сетками (2×2, 3×3) и 3D-сетками (2×2×2); (2) в каждом тесте проверять все публичные поля: `Row()`, `Col()`, `DiagBlocks()`, `OffDiagBlocks()`, `NmbrOfElementsAboveBlockRow()`, `TotalNmbrOfBlocks()`, `NmbrOfNonzerosPerUnitBlock()`, `EqNmbr()`; (3) для каждого элемента каждого вектора вручную вычислить ожидаемое значение из конфигурации (граф связности + blockPattern) и проверить через `CHECK(vec[i] == expected[i])`; (4) покрыть комбинации: полный блок, частичный блок (диагональ), разные eqNmbr (1, 2, 3)
- **Приоритет:** высокий — без поэлементной верификации CRS-структуры баги в индексации (как BUG-019, BUG-013) обнаруживаются только через crash, а не через assertion
- **Связано с:** [[BUG-019]], [[BUG-013]]
- **Статус:** 🔴 ОТКРЫТ

### DEBT-047: `update_isSuccesfullNewtonTrial` побочно инкрементирует Newton counter
- **Описание:** метод `NumericalParameters::update_isSuccesfullNewtonTrial(bool f)` помимо установки `isSuccessfulTrial` безусловно вызывает `update_currentNewtonIterationCount()`, который делает `++currentNewtonIterationCount`. Это побочный эффект: семантически метод устанавливает флаг успешности, но фактически также считает итерации. Из-за этого `PerformNewtonLoop` стартует с `currentNewtonIterationCount = 1` вместо 0 — первый вызов `update_isSuccesfullNewtonTrial(false)` (инициализация) уже инкрементирует counter, несмотря на то что `set_currentNewtonIterationCount(0)` вызван строкой выше
- **Цепочка вызовов:**
  1. `PerformNewtonLoop()` (`ReservoirSimulator.cpp:435`): `set_currentNewtonIterationCount(0)` → counter = 0
  2. `PerformNewtonLoop()` (`ReservoirSimulator.cpp:436`): `update_isSuccesfullNewtonTrial(false)` → `isSuccessfulTrial = false; ++currentNewtonIterationCount` → counter = 1
  3. Newton loop начинает с counter = 1 вместо 0
  4. `IsNewtonIterationContinue()` проверяет `counter < maxNewtonIterNmbr(=12)` → доступно 11 итераций вместо 12
- **Проявление:** Newton loop делает на 1 итерацию меньше, чем задано в `maxNewtonIterNmbr`. При `maxNewtonIterNmbr = 12` доступно 11. Для текущих задач разница не критична — Newton сходится за 1–3 итерации (после фикса BUG-006)
- **Риск:** при сложных задачах потеря одной итерации может привести к ложному wasted trial. При `maxNewtonIterNmbr = 1` (edge case) Newton loop не сделает ни одной итерации
- **Причина:** нарушение Single Responsibility — `update_isSuccesfullNewtonTrial` делает две вещи: (1) устанавливает флаг, (2) считает итерации. Эти действия должны быть разделены
- **Файл:** `HydroSolver/Reservoir/NumericalParameters.h:108`
- **Код:** `void update_isSuccesfullNewtonTrial(bool f) { isSuccessfulTrial = f && CurrentANG_IsAccuracyReached(); update_currentNewtonIterationCount(); }`
- **Действие:** разделить на две операции. Убрать `update_currentNewtonIterationCount()` из `update_isSuccesfullNewtonTrial`, вызывать инкремент явно в Newton loop после `SingleIteration`. Или переименовать метод в `advanceNewtonIteration(bool converged)` и задокументировать оба эффекта. Предпочтительнее первый вариант (разделение), но требует проверки всех call sites
- **Приоритет:** низкий (после BUG-006 Newton делает 1–3 итерации, потеря одной не критична)
- **Связано с:** [[bug-006 solve-converged-always-true]]
- **Обнаружено при анализе:** BUG-006
- **Статус:** 🔴 ОТКРЫТ

### DEBT-048: Неполный Якобиан скважины — отсутствует ∂P_well/∂P_res
- **Описание:** в `AddWellToMatrix` (Wells.cpp) Якобиан не содержит производную `∂P_well/∂P_res`. Место помечено в коде: `// derivative of P_Well /////!!!!!!!!!/////////`. P_well вычисляется в `SetRefWellPressure` как функция всех P_res(l), но эта зависимость не отражена в матрице. Newton сходится линейно вместо квадратично, что увеличивает невязку баланса масс (0.15%–3.4% после фикса BUG-020)
- **Формула:** `P_well = (−Q + Σ ρ_mix·PI·P_res) / (Σ ρ_mix·PI)`, значит `∂P_well/∂P_res(l) = ρ_mix(l)·PI(l) / Σ ρ_mix·PI`, где `PI(l) = factor(l)·OverallMobility(l)`. Нужно добавить `∂production/∂P_well · ∂P_well/∂P_res` в блоки [oil,P] и [water,P]
- **Файл:** `HydroSolver/Reservoir/Well/Wells.cpp:55-56`
- **Проявление:** Newton сходится линейно вместо квадратично для скважинных ячеек. Не блокирует BUG-020 (невязка баланса масс была из-за BalanceOil/CurOilDebit_Num, исправлено)
- **Действие:** реализовать ~10 строк в цикле Якобиана — вычислить `dPw_dPres(l)`, добавить вклад `-ρ·F·factor·λ·dPw_dPres` в блоки `matrixBlockPerPerforation[l][1]` и `[3]`
- **Приоритет:** средний (улучшает сходимость, не блокирует корректность)
- **Связано с:** [[BUG-020 несогласованность единиц расхода в формуле Писмана]]
- **Обнаружено при реализации:** BUG-020
- **GitHub:** [#17](https://github.com/ArturSalamatin/GDM/issues/17)
- **Ветка:** refactor/debt-048/well-jacobian-dpwell
- **План:** [[debt-048 well-jacobian-dpwell]]
- **Статус:** ✅ исправлено 2026-07-07. Newton итерации CPR1-3: −30%

### DEBT-049: Неполный якобиан скважины — отсутствует ∂P_well/∂Sw
- **Описание:** P_well зависит от Sw(l) через OverallMobility(l) (в знаменателе и числителе формулы Писмана) и через F_Oil(l) (в ρ_mix). Производная ∂P_well/∂Sw(l) не учтена в блоках [0] и [2] якобиана. Формула длиннее чем для ∂P_well/∂P_res: включает dOverallMobility/dSw, dF_Oil/dSw, drhoMix/dSw
- **Файл:** `HydroSolver/Reservoir/Well/Wells.cpp:51-52`
- **Проявление:** неполные блоки [0] и [2] — линейная сходимость Newton по Sw для скважинных ячеек
- **Действие:** вывести формулу ∂P_well/∂Sw(l), реализовать аналогично DEBT-048
- **Приоритет:** низкий → отложен. После DEBT-048 (−30% Newton) остаточный вклад ∂P_well/∂Sw оценивается в единицы процентов: конкурирует с уже учтённым ∂λ/∂Sw·dP, а для однооперфорационных скважин dP≈0. Делать при рефакторинге якобиана
- **Связано с:** [[debt-048 well-jacobian-dpwell]]
- **Обнаружено при анализе:** DEBT-048
- **Статус:** 🟡 ОТКРЫТ (отложен)

### DEBT-050: Незаполненный блок ∂F_OilWellBalance/∂Sw для инжектирующих перфораций
- **Описание:** в цикле якобиана (`Wells.cpp:83-86`) блок `else { // derivative of balanced oil }` пуст. Для перфораций с dP < 0 (вода течёт из скважины в пласт) F_Oil определяется через F_OilWellBalance, а не через ячейку. Производная F_OilWellBalance по Sw не учтена
- **Файл:** `HydroSolver/Reservoir/Well/Wells.cpp:83-86`
- **Проявление:** неточный якобиан для инжектирующих перфораций — линейная сходимость Newton
- **Действие:** вывести ∂F_OilWellBalance/∂Sw(l), реализовать в блоке else
- **Приоритет:** низкий → отложен. В текущих сценариях инжекторы закачивают чистую воду (F_Oil=0), производная зануляется. Станет актуально при WAG или многофазной обратной закачке
- **Связано с:** [[debt-048 well-jacobian-dpwell]]
- **Обнаружено при анализе:** DEBT-048
- **Статус:** 🟡 ОТКРЫТ (отложен)

### DEBT-051: AMGCL — перевод на git submodule (форк)
- **Описание:** библиотека AMGCL хранится как локальная копия в `HydroSolver/AMGSolver/amgcl/`. Нужно создать форк `ddemidov/amgcl` на GitHub (ArturSalamatin/amgcl), подключить как git submodule в корне проекта (аналогично Eigen, GridEngine). Обновить `CMakeLists.txt`: include path → `amgcl` вместо `${HYDRO}/AMGSolver/amgcl`
- **Ветки форка:** (1) `experimental/master` — чистый upstream, без патчей, точка входа для submodule; (2) `experimental/patches` — точечные патчи (ilu0 fallback, cpr fallback) с детальным описанием проблемы; (3) `experimental/solvers` — новые методы решения СЛАУ
- **Действие:** форк → submodule add → CMake → сборка + тесты
- **Приоритет:** высокий (блокирует DEBT-052, DEBT-053, FEAT-010/011)
- **Связано с:** [[DEBT-007]], [[локальные патчи AMGCL для GDM]]
- **План:** [[debt-051 amgcl-submodule-and-solvers]]
- **GitHub:** [#18](https://github.com/ArturSalamatin/GDM/issues/18)
- **Ветка:** refactor/debt-051/amgcl-submodule
- **Статус:** ✅ реализовано 2026-07-08

### DEBT-052: Патчи AMGCL — ветка experimental/patches
- **Описание:** перенести существующие патчи (BUG-002: ilu0.hpp fallback D[i]=1, cpr.hpp fallback d=1) в ветку `experimental/patches` форка amgcl. Каждый патч — отдельный коммит с детальным описанием: какую проблему решает, при каких условиях срабатывает (Sw=0, zero pivot), физическая причина (вырожденный блок Якобиана по насыщенности). Патчи — страховка на время разработки новых солверов. Если новые солверы решают проблему Sw=0 — патчи становятся не нужны
- **Действие:** ветка от experimental/master → коммиты с патчами → описания в commit messages
- **Зависимости:** DEBT-051
- **Приоритет:** средний
- **Связано с:** [[BUG-002]], [[zero pivot в ILU0 при скалярном CPR на двухфазном Якобиане]]
- **GitHub:** [#19](https://github.com/ArturSalamatin/GDM/issues/19)
- **Ветка:** refactor/debt-052/amgcl-patches
- **Статус:** ✅ реализовано 2026-07-08

### DEBT-053: SolverFactory — compile-time выбор конфигурации СЛАУ
- **Описание:** сейчас солверная конфигурация захардкожена через `using CPRPrecond`, `using CPRSolver` в `LinearProblem.h`. В бенчмарке (`test_amgcl_benchmark.cpp`) уже есть 4 конфигурации через дублирование `using`. Нужно обобщить: CMake-опция (`-DGDM_SOLVER=...`) переключает `using`-алиасы в `LinearProblem.h`, без runtime-полиморфизма
- **Действие:** CMake option + `#ifdef`-блоки в LinearProblem.h или отдельный SolverConfig.h
- **Зависимости:** DEBT-051
- **Приоритет:** высокий
- **Связано с:** [[возможности amgcl для блочных СЛАУ]]
- **Конфигурации для тестирования:**
  1. CPR<AMG<aggregation, ilu0>, ilu0> + lgmres — текущий baseline
  2. CPR<AMG<aggregation, ilu0>, ilu0> + bicgstab — BiCGStab дешевле по памяти, стандартный выбор для FIM
  3. CPR<AMG<smoothed_aggregation, ilu0>, ilu0> + lgmres — лучшая интерполяция в AMG, дороже setup
  4. CPR-DRS<AMG<aggregation, ilu0>, ilu0> + lgmres — Dynamic Row Summing, улучшает decoupling при Sw→0
  5. ILU0 (без AMG) + lgmres — уже есть в бенчмарке, baseline для сравнения
- **Ключевой тест:** five-spot Sw_init=0 без патчей — какие конфигурации проходят?
- **GitHub:** [#20](https://github.com/ArturSalamatin/GDM/issues/20)
- **План:** [[debt-053 solver-factory]]
- **Ветка:** refactor/debt-053/solver-factory
- **Статус:** ✅ реализовано 2026-07-09

### DEBT-054: Выделить JacobianAssembler из ReservoirSimulator
- **Описание:** перенести `fillMatrixBlockRow`, `AccountForBoundaryConditions` (~240 строк inline в .h) и well contributions из `AssembleMyProblem` в отдельный класс `JacobianAssembler`. Единственная ответственность: сборка матрицы Якобиана и правой части для системы уравнений фильтрации
- **Зависимости:** нет (самое внутреннее ядро, зависит только от OilField + LinearProblem)
- **Действие:** (1) создать JacobianAssembler.h/.cpp с методом `Assemble(loc_tau, nextTimeMoment, grid, problem, refPressure, wells)`; (2) unit-тесты: побитовое сравнение матрицы/RHS с текущим `sim.AssembleMyProblem()`; (3) `ReservoirSimulator::AssembleMyProblem` делегирует `assembler_.Assemble()`; (4) удалить `fillMatrixBlockRow`, `AccountForBoundaryConditions`, `CheckForNAN` из ReservoirSimulator
- **Файлы:** создать `HydroSolver/Reservoir/JacobianAssembler.h`, `JacobianAssembler.cpp`; изменить `ReservoirSImulator.h`, `ReservoirSimulator.cpp`, `CMakeLists.txt`
- **Тесты:** ~4 unit-теста (1D/3×3/with wells/Sw=0), каждый сравнивает новый assembler со старым побитово
- **Приоритет:** высокий
- **Связано с:** [[debt-003 refactoring-reservoir-simulator]] (этап 1)
- **Блокирует:** DEBT-055
- **GitHub:** [#23](https://github.com/ArturSalamatin/GDM/issues/23)
- **Ветка:** refactor/debt-054/jacobian-assembler
- **План:** [[debt-054 jacobian-assembler]]
- **Статус:** ✅ реализовано 2026-07-13

### DEBT-055: Выделить NewtonSolver из ReservoirSimulator
- **Описание:** перенести `PerformNewtonLoop`, `SingleIteration`, `UpdateGrid` в отдельный класс `NewtonSolver`. Единственная ответственность: нелинейная итерация Ньютона (сборка → решение СЛАУ → проверка сходимости → обновление состояния). Владеет `JacobianAssembler`
- **Зависимости:** DEBT-054 (JacobianAssembler)
- **Действие:** (1) создать NewtonSolver.h/.cpp с методом `Solve(...)` → `NewtonResult{converged, newton_iters, amg_solves, total_amg_iters}`; (2) unit-тесты: сходимость на однородной сетке (1 итерация), совпадение с `sim.PerformNewtonLoop`, rollback при расхождении; (3) `ReservoirSimulator::PerformNewtonLoop` делегирует `newton_solver_.Solve()`; (4) оставить `AssembleMyProblem`/`UpdateGrid` как делегирующие методы (backward compat для test_amgcl_benchmark)
- **Файлы:** создать `HydroSolver/Reservoir/NewtonSolver.h`, `NewtonSolver.cpp`; изменить `ReservoirSImulator.h`, `ReservoirSimulator.cpp`, `CMakeLists.txt`
- **Тесты:** ~3 unit-теста
- **Приоритет:** высокий
- **Связано с:** [[debt-003 refactoring-reservoir-simulator]] (этап 2)
- **Блокирует:** DEBT-057
- **GitHub:** [#24](https://github.com/ArturSalamatin/GDM/issues/24)
- **Ветка:** refactor/debt-055/newton-solver
- **План:** [[debt-055 newton-solver]]
- **Статус:** ✅ реализовано 2026-07-13

### DEBT-056: Выделить MassBalanceTracker из ReservoirSimulator
- **Описание:** перенести `MassBalance`, аккумуляторные поля (`prevOil, curOil, accumOil, accumOilOutFlux, accumDebet, prevWater, curWater, accumWater, accumWaterOutFlux, accumWaterDebet, curTime` — 11 полей) и `GetOverallBalance` в отдельный класс `MassBalanceTracker`. Единственная ответственность: учёт массы нефти и воды
- **Зависимости:** нет (параллелен DEBT-054/055, но логически проще делать после)
- **Действие:** (1) создать MassBalanceTracker.h/.cpp с `Initialize(oil, water)`, `Update(dt, ...)`, `GetState()`, `GetOverallBalance()`; (2) unit-тесты: аккумуляция за несколько шагов, совпадение с текущим sim; (3) заменить 11 полей в ReservoirSimulator на `MassBalanceTracker balance_tracker_`
- **Файлы:** создать `HydroSolver/Reservoir/MassBalanceTracker.h`, `MassBalanceTracker.cpp`; изменить `ReservoirSImulator.h`, `ReservoirSimulator.cpp`, `CMakeLists.txt`
- **Тесты:** ~3 unit-теста
- **Приоритет:** средний
- **Связано с:** [[debt-003 refactoring-reservoir-simulator]] (этап 3)
- **GitHub:** [#25](https://github.com/ArturSalamatin/GDM/issues/25)
- **Ветка:** refactor/debt-056/mass-balance-tracker
- **План:** [[debt-056 mass-balance-tracker]]
- **Статус:** ✅ реализовано 2026-07-13

### DEBT-057: Выделить TimeIntegrator из ReservoirSimulator
- **Описание:** перенести `Solve` (time-stepping loop) в отдельный класс `TimeIntegrator`. Единственная ответственность: цикл по временным шагам с управлением адаптивным шагом. Владеет `NewtonSolver`. Используя коллбэки для операций, зависящих от ReservoirSimulator (AcceptState, AddFlowFieldSnapShot, MassBalance)
- **Зависимости:** DEBT-055 (NewtonSolver), DEBT-056 (MassBalanceTracker)
- **Действие:** (1) создать TimeIntegrator.h/.cpp с `Integrate(timeMoments, grid, problem, ...)` и `StepCallbacks{on_accept, on_balance}`; (2) unit-тесты: идентичные поля P/Sw после интегрирования, корректная обработка wasted trials; (3) `ReservoirSimulator::Solve` делегирует `time_integrator_.Integrate(...)`, ReservoirSimulator становится тонким фасадом (~300-400 строк)
- **Файлы:** создать `HydroSolver/Reservoir/TimeIntegrator.h`, `TimeIntegrator.cpp`; изменить `ReservoirSImulator.h`, `ReservoirSimulator.cpp`, `CMakeLists.txt`
- **Тесты:** ~2 unit-теста
- **Приоритет:** средний
- **Связано с:** [[debt-003 refactoring-reservoir-simulator]] (этап 4)
- **GitHub:** [#26](https://github.com/ArturSalamatin/GDM/issues/26)
- **План:** [[debt-057 time-integrator]]
- **Ветка:** refactor/debt-057/time-integrator
- **Статус:** ✅ реализовано 2026-07-14
