---
tags:
  - планирование
  - статус
date: 2026-06-17
---

# Текущие приоритеты

## Фаза 0: Инициализация проекта — ✅ завершена
- Git-репозиторий ✅
- Obsidian vault ✅
- CLAUDE.md ✅
- Дедупликация legacy ✅ (2026-06-16)
- Актуальная версия определена: `PCNAC\Repo\GDM\` (конец 2022)
- Исходники скопированы в корень проекта ✅ (2026-06-16):
  - `HydroSolver/` (126 исходников + AMGSolver)
  - `Shared0/` (18 утилит)
  - `GridEngine/` (12 файлов, парсер GRDECL)
  - `MatLab/` (14 скриптов визуализации)
- Ревизия legacy-кода завершена ✅ (2026-06-16), см. [[ревизия legacy-кода декабрь 2022]]

## Фаза 1: CMake + сборка без внешних зависимостей — ✅ завершена (2026-06-16)
- CMakeLists.txt с явным списком исходников ✅
- PostgreSQL (pqxx) убрана из stdafx.h ✅
- GEOS убрана (Point.h, FlowField.h, GeosPoint.h — заглушки) ✅
- DevelopedHorizon.h вынесен из HorizonFactory.h ✅
- Синтетический `src/main.cpp` (20×20×1 однородный пласт, без скважин) ✅
- amgcl подключен header-only с AMGCL_NO_BOOST ✅
- UniversalSVWriter/Parser исключены (кодировка cp1251 несовместима с /utf-8) ✅
- SaveFlowField2File и LoadFlowFieldFromFile заглушены ✅
- **Результат:** Debug-сборка проходит, gdm.exe запускается, 10 шагов по 10 дней, Ньютон сходится за 1 итерацию

## Фаза 2: Тестовый фреймворк — ✅ завершена (2026-06-17)
- Catch2 v3.8.0 через FetchContent ✅
- `gdm_tests` executable с GDM_CORE_SOURCES ✅
- `test_helpers.h`: `make_uniform_horizon()`, `default_num_params()`, `add_simple_well()` ✅
- **22 теста, все проходят в Release (28 сек)**

### Smoke (#00) — ✅
- Simulator creates/runs, saturation bounds [0,1]

### Stationary pressure (#01) — ✅
- Trivial equilibrium (P_init = RefPressure)
- Relaxation to boundary pressure
- Symmetry of uniform IC

### Mass balance (#02) — ✅
- Closed reservoir conserves mass
- Expected initial mass values
- Open boundary flow decreases mass
- Physical constraints: S_w + S_o = 1, P > 0

### Buckley–Leverett (#03) — ✅ (аналитика), ⚠️ (симуляция)
- Аналитические формулы f_w, df_w, find_Swf, analytical_profile ✅
- 3 теста: f_w свойства, df_w vs конечные разности, Welge конструкция ✅
- **Обнаружен баг:** Newton divergence при закачке воды через скважину
  - `ReverseState()` не откатывает состояние скважины → NaN propagation
  - Описан в [[Newton divergence при закачке воды через скважину]]
  - Скрытый тест `[.wells]` документирует проблему

### Компоненты (#04) — ✅
- Corey relperm: boundary, intermediate, monotonicity, derivative
- Fractional flow: boundary, S-shape + monotonicity
- Trivial solve: 0 wasted trials
- Simulator relperm: S_w + S_o = 1 из GetFields
- Grid size: P.size() == Nx*Ny*Nz

### Five-spot 2D (#05) — ✅
- 21×21×1, 5 скважин (1 INJ + 4 PROD), 500 дней
- Newton сходится, saturation bounds, 4-fold symmetry
- MRST шаблон подготовлен (MatLab/)
- Скрипт сравнения GDM vs MRST (MatLab/)
- См. [[five-spot сравнение с MRST]], [[таблица единиц GDM vs MRST]]

## Фаза 2.5: Визуальная верификация — ✅ завершена (2026-06-17)
- `WaterContourFlux()` + водные аккумуляторы в `MassBalance()` ✅
- `SimulationCase` (абстрактная фабрика) + `SingleInjectorCase` ✅
- Тест `test_visual_verification.cpp`: 20 шагов по 10 дней, single injector ✅
  - Баланс масс нефти: невязка < 1e-15 (машинная точность)
  - Баланс масс воды: невязка < 1e-4 кг при M ~ 1e8 кг
  - Физ. ограничения: 0 ≤ S_w ≤ 1, P > 0 на каждом шаге
- Экспорт: 21 snapshot CSV + `mass_balance.csv` + `metadata.json`
- Python-скрипт `scripts/animate_fields.py`: анимация S_w + P, график баланса
- Обнаружена и исправлена неверная знаковая конвенция: `ΔM + outflux - debet_accum = 0`
  (не `+debet_accum`, т.к. `accumDebet = -∫(Debit·dt)`)
- Сеточная сходимость: 11×11, 21×21, 41×41 на области 250×250 м ✅
  - Oil mass сходится к 72260015 кг (Δ = 302 → 0.3)
  - S_w, P в probe: разности уменьшаются (dSw: 0.072 → 0.026, dP: 1.17 → 0.48)
  - `SingleInjectorCase` параметризован по Nx/Ny
  - `scripts/plot_convergence.py`: сравнение полей + кривые сходимости
- Двускважинный сценарий: INJ + PROD, 63×63, 730 дней, Q=50 м³/сут ✅
- `run_case()` обобщён на произвольный `SimulationCase`
- Маркеры скважин в анимации (▼ INJ, ▲ PROD) ✅
- **25 тестов, все проходят в Release**

## Фаза 2.6: Линии тока — ✅ завершена (2026-06-18)
- `Trajectory.cpp` добавлен в CMake (ядро трассировки) ✅
- `test_streamlines.cpp`: single injector (21×21, 200 дней) + two wells (21×21, 200 дней) ✅
- Экспорт CSV: `streamlines.csv`, `velocity_field.csv`, `metadata.json` ✅
- Python-визуализация: `scripts/plot_streamlines.py` (streamlines + quiver plot) ✅
- Используются `PhasePortrait`, `Trajectory`, `SomeFlowField` из `Anomaly/FlowField/`
- `Anomaly.cpp` (legacy WellSignals/GeoJSON) не задействован
- **27 тестов, все проходят в Release**

## Фаза 2.7: Скважины с переменным дебитом — ✅ завершена (2026-06-18)
- `WellScheduleBuilder` — fluent-фабрика для MER-истории скважины ✅
- `VariableDebitCase` — универсальный SimulationCase для переменных сценариев ✅
- 4 теста: два режима, shut-in, усиление закачки, попеременная работа ✅
- Визуальная верификация: animation.gif + mass_balance.png для всех сценариев ✅
- Анализ CleanMER_record: mid-sequence нули сохраняются, shut-in безопасен ✅
- **30 тестов, все проходят в Release**

## Фаза 3: Рефакторинг ядра солвера — 📋 следующая
- Исправить Newton divergence при wasted trial + скважины
- Удалить Shared0/ (дубль Utils/)
- Заменить wstring на string (убрать Windows-зависимость из ядра)
- Ввести файловый ввод (JSON/YAML) вместо заглушенных DB-фабрик
- Выделить чистый solver API из ReservoirSimulator
- Профилирование: amgcl setup доминирует (14–15% времени)

## Фаза 4: Расширение модели — 📋 планируется
- Z-направление (сейчас отключено, Nz=1)
- Модель скважин (Peaceman)
- Режим BHP скважины (фиксированное забойное давление)
- Закрытые границы (no-flow) — требует корректной регуляризации давления, см. [[закрытые границы требуют корректной регуляризации давления]]

## Наблюдения
- Ньютон сходится за 1 итерацию при однородном пласте без скважин
- `solve` = 0.000 s, `setup` = 14–15% → amgcl тратит всё на setup, решение мгновенно (400 ячеек)
- Five-spot 21×21: AMG = 87% времени, Release в 10x быстрее Debug
- Скважины работают корректно при S_w_init > 0 (five-spot с oil_saturation = 1.0 тоже работает)
- Проблема Newton divergence проявляется в специфичных условиях (чистая нефть + закачка воды + wasted trial)
